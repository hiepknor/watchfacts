services:
  watchfacts-mcp:
    ports:
      - "127.0.0.1:8765:8765"
    networks:
      - default
      - openwa

networks:
  openwa:
    external: true
    name: ${OPENWA_DOCKER_NETWORK:-openwa-network}
